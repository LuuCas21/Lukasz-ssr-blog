<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>Blog - LukaSZ</title>
</head>
<body>
    <div class="container">
        <h2 class="mb-4 mt-4 display-2"><%= articles.title %></h2>
        <h6 class="text-muted mb-2">Posted · <%= articles.createdAt.toLocaleDateString() %></h6>
        <h6 class="text-muted mb-2">Reading time · <%= readingEstimator.text %></h6>
        <h6 class="text-muted mb-2">Author · <%= articles.createdBy %></h6>
 
        <a href="/" class="btn btn-secondary mt-4">All Articles</a>
        <% if (adminAccess) { %>
            <a href="/articles/edit/<%= articles.id %>" class="btn btn-info mt-4">Edit</a>
        <% } %>
        <div class="mt-4 mb-4">
            <%- articles.sanitizedHTML %>
        </div>
    </div>
</body>
</html>